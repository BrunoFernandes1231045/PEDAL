{
  "name": "pedal",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "npx serve . --listen 3000",
    "test:backend": "cd backend && npm test",
    "install:backend": "cd backend && npm install"
  }
}
